{
  "$schema": "https://railway.app/railway.schema.json",
  "services": [
    {
      "name": "app",
      "healthcheckPath": "/api/health",
      "healthcheckTimeout": 100,
      "buildCommand": "cd ./apps/frontend && bun install && bun run build && cd ../backend && go build -o ./main ./cmd/todo/main.go",
      "startCommand": "./main",
      "rootDirectory": "apps/backend"
    }
  ]
}